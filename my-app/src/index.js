import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // App.js를 호출

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
